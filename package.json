{
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "verify": "pnpm --dir page-blocks verify && pnpm run verify:examples",
    "verify:examples": "pnpm --dir examples/vite typecheck && pnpm --dir examples/vite build && pnpm --dir examples/vite-blueprints typecheck && pnpm --dir examples/vite-blueprints build && pnpm --dir examples/vite-travel-campaign typecheck && pnpm --dir examples/vite-travel-campaign build",
    "publish-packages": "turbo run build && changeset version && changeset publish"
  },
  "devDependencies": {
    "eslint": "^8.48.0",
    "prettier": "^3.0.3",
    "syncpack": "^11.2.1",
    "tsconfig": "workspace:*",
    "turbo": "latest"
  },
  "packageManager": "pnpm@8.6.10",
  "name": "page-blocks-repo",
  "dependencies": {
    "@changesets/cli": "^2.26.2"
  }
}
